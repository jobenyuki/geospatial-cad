import * as THREE from "three";

import { Tool } from "./tool.js";

export class PenTool extends Tool {
  constructor(cad) {
    super(cad);
  }

  /**
   * Initialize
   */
  init() {}

  /**
   * Update
   */
  update() {}

  /**
   * Dispose
   */
  dispose() {}
}
